# macOS / editor junk
.DS_Store
Thumbs.db
*.swp
.vscode/
.idea/

# Node (test tooling only — the app itself has no dependencies)
node_modules/
npm-debug.log*
package-lock.json

# Module copies the test harness generates at runtime
tests/.tmp/

# Screenshots from `npm run test:layout:shots`
tests/.shots/
